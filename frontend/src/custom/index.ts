/*
 * Registro das telas CUSTOM do app construído sobre o framework.
 * Importe seus componentes e registre-os pelo nome cadastrado em
 * vision.nm_component:
 *
 *   import { register } from '../registry'
 *   import Treinar from './Treinar'
 *   register('treinar', Treinar)
 *
 * Este arquivo é importado pelo main.tsx — o framework em si não registra nada.
 */
export {}
